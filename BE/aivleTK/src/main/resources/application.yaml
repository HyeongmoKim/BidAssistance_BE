server:
  port: ${PORT:8080}
spring:
  application:
    name: aivleTK
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  # 마지막날에 db 주소 바꿔야한다고 말좀 해주세요 이거 공용이라
  datasource:
    url: jdbc:mysql://aivle08be-server.mysql.database.azure.com:3306/aivleTK?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&sslMode=REQUIRED
    username: cufblosigq
    password: beV9i3$DWXZMhLqn
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
app:
  cors:
    allowed-origins: "http://localhost:5173,https://calm-tree-0e8ee630f.4.azurestaticapps.net"

jwt:
  secret:
    key: "aivleaivleaivleaivleaivle1234567aivleaivleaivleaivleaivle1234567"
azure:
  storage:
    connection-string: ${AZURE_STORAGE_KEY}
    container-name: "uploads"
